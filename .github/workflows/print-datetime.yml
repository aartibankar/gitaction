name: Print Date & Time (Every 15 Minutes)

on:
  schedule:
    - cron: "*/15 * * * *"   # Runs every 15 minutes (UTC time)
  workflow_dispatch:          # Optional: allows manual run from Actions tab

jobs:
  show-datetime:
    runs-on: ubuntu-latest

    steps:
      - name: Print current date and time
        run: |
          echo "🕒 Current UTC Date & Time:"
          date -u
